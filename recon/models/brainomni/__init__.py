"""BrainOmni-based alignment models.

This subpackage hosts models that consume BrainOmni features and project
them to the 768-dim semantic space. All models in this subpackage
require the ``brainomni`` extra (see ``docs/standards/07-dependencies.md``).

Currently placeholder. See ``recon.encoders.brainomni`` for the encoder
implementation. The alignment heads (MLP / IB / Attention) will live here.
"""