# Cluster-side paths (production)
# This is the default config used on the HPC cluster.

data_root: /home/test/reconstruction             # raw BIDS data (mydata/)
processed_root: /home/test/reconstruction/results  # preprocessed results
results_dir: /home/test/reconstruction/results
pretrained_root: /home/test/reconstruction/pretrained

# Specific paths
gpt_path: ${paths.pretrained_root}/gpt2-chinese
brainomni_path: ${paths.pretrained_root}/brainomni
