# ───────────────────── Python ─────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ───────────────────── Virtual envs ─────────────────────
.venv/
venv/
env/
ENV/
.python-version

# ───────────────────── Test / coverage ─────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.cache
.tox/
.nox/

# ───────────────────── Type checking ─────────────────────
.mypy_cache/
.pyre/
.pytype/

# ───────────────────── Lint / format ─────────────────────
.ruff_cache/
.dmypy.json
dmypy.json

# ───────────────────── Jupyter ─────────────────────
.ipynb_checkpoints/
*.ipynb

# ───────────────────── Data (NEVER commit) ─────────────────────
# These are all large binary formats used by brain imaging data.
*.npy
*.npz
*.pt
*.pth
*.pkl
*.fif
*.nii
*.nii.gz
*.dtseries.nii
*.h5
*.hdf5
*.pkl

# ───────────────────── Trained models / checkpoints ─────────────────────
checkpoints/
ckpt/
models/pretrained/
*.ckpt

# ───────────────────── Logs ─────────────────────
logs/
*.log
*.out
*.err

# ───────────────────── Weights & Biases ─────────────────────
wandb/
.wandb/

# ───────────────────── MLflow ─────────────────────
mlruns/
mlartifacts/

# ───────────────────── Hydra outputs ─────────────────────
outputs/
multirun/

# ───────────────────── Environment / secrets ─────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# ───────────────────── IDE / editor ─────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ───────────────────── Project-specific ─────────────────────
# Our local result directories (configurable per host)
results/
scratch/
tmp/

# ───────────────────── MkDocs / docs build ─────────────────────
site/
docs/_build/

# ───────────────────── Pre-commit cache ─────────────────────
.pre-commit-cache/

# ───────────────────── git-lfs (when adopted) ─────────────────────
# (uncomment if using git-lfs)
# *.pt filter=lfs diff=lfs merge=lfs -text
# *.npy filter=lfs diff=lfs merge=lfs -text
# *.h5 filter=lfs diff=lfs merge=lfs -text
# Local test artifacts
tests/tmp_results/

# CLI outputs
logs/
decoded/
